# Sensor Gateway (Pi 1) Dependencies

# Core system
RPi.GPIO>=0.7.0
gpiozero>=1.6.2

# Hardware sensor drivers
smbus2>=0.4.1        # ADS1115 I2C ADC (for pH sensor)

# Communication (CoAP)
aiocoap>=0.4.3
cryptography>=3.4.6

# Data handling
numpy>=1.21.0

# Logging and config
python-dotenv>=0.19.0

# Testing
pytest>=6.2.4
pytest-asyncio>=0.18.0
