# Forensic Guardian (Pi 2) Dependencies

# Communication (CoAP)
aiocoap>=0.4.3
cryptography>=3.4.6

# ML / Anomaly Detection
scikit-learn>=0.24.2
numpy>=1.21.0
joblib>=1.1.0

# TFLite – install tflite-runtime on Pi, tensorflow on desktop
# tflite-runtime>=2.6.0   # uncomment on Raspberry Pi
# tensorflow>=2.6.0       # uncomment on desktop for training

# Web Dashboard
flask>=2.0.0

# Evidence / Forensics (cryptography already above)

# Logging and config
python-dotenv>=0.19.0

# Data (for ML training only – optional on Pi)
pandas>=1.3.0

# Testing
pytest>=6.2.4
pytest-asyncio>=0.18.0

# Testing
pytest>=6.2.4
pytest-asyncio>=0.18.0
